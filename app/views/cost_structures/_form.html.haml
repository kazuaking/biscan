= simple_form_for([@business_model_canvase, @cost_structure]) do |f|
  .form-inputs
    = f.association :business_model_canvase
    = f.input :name
    = f.input :description
    = f.input :updated_by

  .form-actions
    = f.button :submit
